import React from "react";

const Loader = () => {
  return (
    <div className="loader-modal-open loader-modal">
      <div className="modal-loader"></div>
    </div>
  );
};

export default Loader;
